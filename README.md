# physical_distancing_detection
Detect and alarm visually when physical distancing protocol is breached by humans

Place the input video file in the directory that contains code and pass the input to the script using -i argument else default input file name will be used.

Output video will be saved in the same directory, sepcify the name of the output file name using -o argument else default name will be used.
